spring.h2.console.enabled=false
spring.jpa.show-sql=false
server.forward-headers-strategy=framework

# In production, prefer setting SPRING_DATASOURCE_URL to PostgreSQL/MySQL.
# If not provided, the base application.properties fallback will still use file-based H2.
